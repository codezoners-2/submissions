void setup()
{
size(600,600);
background(255);
}

void draw()
{
  float randX = random(-30,30);
  float randY = random(-30,30);
  float randSize = random(5,15);
  fill(0,0,random(100,255),random(100,255));
  ellipse(mouseX+randX, mouseY+randY, randSize, randSize);
}
